import { ValidationFormatDirective } from './validation-format.directive';

describe('ValidationFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationFormatDirective();
    expect(directive).toBeTruthy();
  });
});
